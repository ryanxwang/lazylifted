[partial-action-model]
successor-generator = "full-reducer"
graph-compiler = "rslg"
validate = false
group-partial-actions = true

[partial-action-model.model]
ranker-name = "lp"

[partial-action-model.wl]
iters = 3
set-or-multiset = "multiset"
